interface:
  display_name: "Docs Maintain"
  short_description: "Incrementally or periodically revalidate evidence"
  default_prompt: "Incrementally or periodically maintain evidence-backed knowledge from its Domain Manifest, preserving validation_report metadata and routing changed claims to resynthesis without implying a scheduler or editing immutable publications."
