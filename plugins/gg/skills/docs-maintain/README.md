# Capability matrix

`docs-maintain` requires a valid Claim/Evidence store, repository access, and index. Runtime adapters are optional but each missing adapter narrows permissible conclusions. Every run performs capability preflight; no scheduler is implied by this skill.
