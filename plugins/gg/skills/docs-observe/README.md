# Capability matrix

`docs-observe` verifies static implementation using pinned local repositories, versioned configuration, contracts, tests, and approved source documents. RAG is optional and non-authoritative. Runtime deployment, effective configuration, experiments, traces, and metrics are not verified; dependent Claims become `requires-runtime-evidence`.
